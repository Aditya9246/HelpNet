<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.Helpnet" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Use basic theme attributes without custom colors -->
    </style>

    <!-- Light theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Basic theme without color attributes -->
    </style>

    <!-- Dark theme -->
    <style name="AppTheme.Dark" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Basic theme without color attributes -->
    </style>
</resources>